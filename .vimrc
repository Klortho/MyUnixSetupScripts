" Got some of these from the sample vimrc, :r $VIMRUNTIME/vimrc_example.vim

" Use Vim settings, rather than Vi settings (much better!).
" This must be first, because it changes other options as a side effect.

set nocompatible

:set ignorecase
:set expandtab
:set shiftwidth=2
:set softtabstop=2
:set incsearch
